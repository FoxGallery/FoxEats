import { Screen, Text } from '@foxeats/ui-mobile';

export default function BrowseScreen() {
  return (
    <Screen>
      <Text variant="display" className="mt-6">Explorer</Text>
      <Text variant="body" className="mt-1 text-ink-muted">Recherche et filtres → M2</Text>
    </Screen>
  );
}
