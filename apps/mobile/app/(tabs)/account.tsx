import { Screen, Text } from '@foxeats/ui-mobile';

export default function AccountScreen() {
  return (
    <Screen>
      <Text variant="display" className="mt-6">Compte</Text>
      <Text variant="body" className="mt-1 text-ink-muted">Profil, adresses, FoxPass, FoxCoins → M1/M11</Text>
    </Screen>
  );
}
